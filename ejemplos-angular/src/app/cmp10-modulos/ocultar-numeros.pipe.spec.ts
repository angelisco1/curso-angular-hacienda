import { OcultarNumerosPipe } from './ocultar-numeros.pipe';

describe('OcultarNumerosPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultarNumerosPipe();
    expect(pipe).toBeTruthy();
  });
});
